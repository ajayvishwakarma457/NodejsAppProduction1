# Enforce package-lock for reproducible builds
package-lock=true

# Strict engine checks
engine-strict=true

# Default to exact versions on manual npm install
# Patch auto-updates are managed via scripts (ncu --target patch)
save-exact=true
